// @ts-ignore
import { useNavScroll } from './';
// import { renderHook, act } from '@testing-library/react-hooks';

// mock timer using jest
// jest.useFakeTimers();

describe('useMyHook', () => {
  it('updates every second', () => {
    expect(true).toBe(true);
  });
});
